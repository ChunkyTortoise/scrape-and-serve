# Scrape-and-Serve Configuration
# Copy to .env and customize as needed

# Demo mode — uses bundled sample data
DEMO_MODE=true

# Streamlit configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_HEADLESS=true
